const pgClient = require('../postgresql')

describe('Unit Test - postgresql client', ()=>{
    test('Initialize - ', async ()=>{
        let exception = null;
        try{
            //TODO: puth test code
        }catch(e){
            exception = e;
        }
        expect(exception).toBeNull();
    })
})